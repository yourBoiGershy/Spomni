# templates/

Assistant file templates — `person.md`, `interaction.md`, `capture-event.md`,
`signal-event.md`, `wakeup.md` — land here with the contracts build wave
(Phase 0 of the build plan). Protected machinery: edits go through
dev-workers, not the main conversation (see CLAUDE.md).

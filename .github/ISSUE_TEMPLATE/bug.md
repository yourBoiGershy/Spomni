---
name: Bug
about: Something behaves wrong
---

**What happened / what you expected**

**Repro** (synthetic data only — never paste real people's names or messages)

**Environment**: macOS version, `bash --version`, `node --version` (if query layer), which connector lanes are enabled.

**Output** of the relevant suite or `sync-scheduler.sh status`, if applicable.

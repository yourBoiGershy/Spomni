## Mission test

<!-- One line: which running cost does this cut? (If it touches trust / care / intent / time, stop — it's out of scope.) -->

## What changed

## How it was verified

<!-- `bash scripts/test-all.sh` output, or the specific suite(s). -->

- [ ] Fixtures and examples are synthetic (no real people, `example.com` addresses)
- [ ] No new send path without a human; no enrichment / scraping
- [ ] Contract or manifest versions bumped if a contract changed
- [ ] `docs/SETUP.md` updated if a new setup step is required

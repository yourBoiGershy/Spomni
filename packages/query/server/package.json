{
  "name": "spomni-query",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Read-only MCP server exposing the Spomni store as query tools.",
  "engines": {
    "node": ">=22.6"
  },
  "bin": {
    "spomni-query": "src/index.ts"
  },
  "scripts": {
    "start": "node --experimental-strip-types src/index.ts"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.30.0",
    "gray-matter": "^4.0.3"
  }
}
